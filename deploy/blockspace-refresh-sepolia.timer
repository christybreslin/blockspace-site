[Unit]
Description=Run the Sepolia census refresh every 3 hours

[Timer]
# Offset one hour from the mainnet refresh (00/3) so the two do not fire at once.
OnCalendar=*-*-* 01/3:00:00
RandomizedDelaySec=120
Persistent=true

[Install]
WantedBy=timers.target
